{
  "name": "digital-toploader",
  "version": "0.24.0",
  "private": true,
  "description": "Digital TopLoader - TCG collection tracker (digitaltoploader.com)",
  "scripts": {
    "import": "cd scripts && node import.js",
    "import:test": "cd scripts && node import.js --test"
  }
}
